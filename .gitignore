node_modules
.next
.env
.env.local
.env.*.local
*.log
.DS_Store

# Local push helper & secrets — never commit
push.sh
.push.env

# Claude Code agent worktrees — local scaffolding, never commit
.claude/worktrees/
